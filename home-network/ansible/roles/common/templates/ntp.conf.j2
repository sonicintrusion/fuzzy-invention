# cat /etc/ansible/templates/ntp.conf
driftfile /var/lib/ntp/ntp.drift

restrict default nomodify notrap nopeer noquery

restrict 127.0.0.1 
restrict ::1

server {{ alias.ntp }} iburst

disable monitor